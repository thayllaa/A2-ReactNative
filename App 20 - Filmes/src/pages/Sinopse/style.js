import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  sinopse: {
    padding: 10
  },
  subTitle: {
    fontSize: 18,
    fontWeight: 'bold'
  },
  text: {
    fontSize: 14,
    color: '#444',
    marginTop: 14,
    marginLeft: 7
  }
})

export { styles }